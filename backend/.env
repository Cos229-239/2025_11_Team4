# Server Configuration
PORT=5000
NODE_ENV=development

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:5173

# PostgreSQL Database
# Option 1: Use DATABASE_URL (recommended for cloud databases like Supabase, Railway, etc.)
DATABASE_URL=postgresql://postgres:cFoDGavQYimPiIXV@db.kypuzijmaztvmgrmswiw.supabase.co:5432/postgres

# Option 2: Or use individual database parameters
# DB_HOST=localhost
# DB_PORT=5432
# DB_NAME=ordereasy
# DB_USER=postgres
# DB_PASSWORD=your_password
# DB_SSL=false

# JWT Configuration
# Generate a strong secret with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=1315611316515151615615151651531515615615313156161661813156156131516156131561516131561613156156131516156
JWT_EXPIRES_IN=7d

# Email Configuration (Optional - for reservation confirmations)
SEND_EMAILS=false
EMAIL_FROM="OrderEasy <noreply@ordereasy.app>"

# SMTP Settings (if SEND_EMAILS=true)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your_smtp_username
SMTP_PASS=your_smtp_password

# Kitchen Dashboard PIN (not recommended - should be moved to database)
# KITCHEN_PIN=1234

# Square Payment Integration
# Get credentials from: https://developer.squareup.com/apps
SQUARE_ACCESS_TOKEN=your_square_access_token_here
SQUARE_LOCATION_ID=your_square_location_id_here
SQUARE_ENVIRONMENT=sandbox
# Values: sandbox (for testing) or production (for live payments)

# Square Webhooks
# Enable or disable webhook processing
SQUARE_WEBHOOK_ENABLED=true
# Secret key used to verify the webhook HMAC signature
SQUARE_WEBHOOK_SIGNATURE_KEY=https://kypuzijmaztvmgrmswiw.supabase.co/functions/v1/square-webhook
# Optional: custom webhook path (default is /api/payments/webhook)
# SQUARE_WEBHOOK_PATH=/api/payments/webhook

# Cleanup job coordination (single runner across instances)
# Arbitrary 32-bit integer used by Postgres advisory lock
CLEANUP_ADVISORY_LOCK_KEY=842150451

# Reservation Policy
# Hours before reservation start when cancellations/refunds are allowed for confirmed reservations
CANCELLATION_WINDOW_HOURS=12

# Reservation duration (minutes) used for overlap/conflict checks
RESERVATION_DURATION_MINUTES=90

